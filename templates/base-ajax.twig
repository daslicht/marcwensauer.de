<div class="container" id="main_container">
		<section id="content" class="content" role="main" class="content-wrapper">
				<div class="row">
					<div class="box">
					{# {{function(	'qtranxf_generateLanguageSelectCode','image')}} #}
					{# {{function( 'do_action','icl_language_selector' )}} #}
					{% include 'includes/language_chooser.twig' %}
	{# 				<div class="col-lg-12">
						<hr>
							<h2 class="intro-text text-center">
								{% if title %}<h1>{{title}}</h1>{% endif %}
							</h2>
						<hr>
					</div> #}
					<div>
						<div class="wrapper {{sidebar_class}}">
							{% block content %}
								Sorry, no content
							{% endblock %}
						</div>
						{% if sidebar %}
							<aside class="layout-sidebar">
								{{sidebar}}
							</aside>
						{% endif %}	
					</div> 
					 <div class="clearfix"></div>
				</div>
			</div>
		</section>
</div>