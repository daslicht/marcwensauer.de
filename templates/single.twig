{% extends "base.twig" %}

{% block content %}
	<div class="content-wrapper">
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section class="article-content">
				<div class="col-lg-12">
                    <hr>
                    <h2 class="intro-text text-center">{{post.title}}
                        {# <strong>business casual</strong> #}
                    </h2>
                    <hr>
					<p class="blog-author text-center">
						<a href="{{post.author.path}}"> {{ post.author.name }} </a> {{ post.post_date|date}} | 	Comment: {{comment_number}}
					</p>
                </div>


				<div class=" text-justify ">
				{# col-md-8 col-md-offset-2 #}
					{{post.content}}
				</div>
			</section>

			<section class="comment">
				<div class="col-lg-12">
					<hr>
	                    <h2 class="intro-text text-center">
	                    	Comments
	                    </h2>
                    <hr>
                    
                    <div class="text-justify ">
                    {# col-md-8 col-md-offset-2 #}
           				{{function('wpsites_comment_form_shortcode')}}
           			
					    {#
			{# 			<nav id="comment-nav-above">
					        <div class="nav-previous">
					        previous
					             {{ function('previous_comments_link') }} 
					        </div>
					        <div class="nav-next">
					            next
					        </div>
					    </nav> #}
{# 						<div class="responses">
							{% for cmt in post.get_comments() %}
								{% include "comment.twig" with {comment:cmt} %}
							{% endfor %}
						</div> #}
						{{ function('comments_template') }}
					</div>
        


				</div>

			</section>
		</article>
	</div><!-- /content-wrapper -->
{% endblock %}
