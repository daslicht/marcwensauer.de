{% extends layout %}

{% block content %}

	<div class="col-lg-12">
		<hr>
			{% if title %}
				<h2 class="intro-text text-center">
					<strong>{{title}}</strong>
				</h2>
			{% endif %}
		<hr>
	</div> 
{# 
	<h2>{{foo}}</h2>
	<p>{{qux}}</p> #}

	{% for post in posts %}
		{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}
	{% endfor %}

	{% include 'pager.twig' %}

{% endblock %}